class Busquedas {
    historial = ['Junin', 'Rosario', 'Buenos Aires'];

    constructor(){
        // Leer DB
    }

    async ciudad (lugar = '') {
        console.log(lugar);

        return []; //retornar ciudad que coincidan con el ingreso de usuario
    }
}

module.exports = Busquedas;